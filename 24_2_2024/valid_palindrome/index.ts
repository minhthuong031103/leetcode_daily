// Step 1: Understanding the Problem
// What is a Palindrome?
// A palindrome is a string that reads the same forward and backward, after:

// Converting to lowercase.
// Removing all non-alphanumeric characters (letters and numbers only).

// Example Walkthrough
// ✅ Example 1:

// Input:  "A man, a plan, a canal: Panama"
// Step 1: Remove special characters → "AmanaplanacanalPanama"
// Step 2: Convert to lowercase → "amanaplanacanalpanama"
// Step 3: Check if reversed is the same → ✅ true

// ❌ Example 2:

// Input:  "race a car"
// Step 1: Remove special characters → "raceacar"
// Step 2: Convert to lowercase → "raceacar"
// Step 3: Check if reversed is the same → ❌ false
